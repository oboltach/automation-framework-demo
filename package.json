{
  "name": "automation-framework-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "cy:run": "cypress run",
    "cy:run:api": "cypress run --spec \"cypress/e2e/api/**/*.cy.js\" --config baseUrl=https://example.com --env BASE_URL=",
    "cy:open:api": "cypress open --config \"specPattern=cypress/e2e/api/**/*.cy.js,baseUrl=https://example.com\" --env BASE_URL="
  },
  "devDependencies": {
    "cypress": "^13.8.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.1.0"
  }
}
